# OpenClaw Execution Prompt: E1.5 Remediation + API Recovery
> Generated: 2026-04-09
> Audited by: OpenClaw (Plumbo) live repo + runtime audit
> Orchestrator: Claude Cowork | Executor: OpenClaw (Plumbo)
> Branch: `fix/e1-5-sports-proposer-2026-04-08`

---

## PMCI Invariants (read on first message of new session)

```
[PMCI invariants: no .env writes; run verify:schema after migrations; new routes in src/api.mjs only; inactive-guard before bulk market changes; never skip npm run verify:schema]
```

---

## Situation Summary

E1.1 through E1.4 are complete and committed. E1.5 is the only gate blocking E2 (crypto).

**What the audit found (2026-04-09, verified runs):**

| Check | Result |
|-------|--------|
| `npm run verify:schema` | PASS |
| `npm run pmci:smoke` | PASS — 71,750 markets, 415,249 snapshots, 3,119 families, 124 links |
| `npm run pmci:propose:sports` | FAIL — `considered=0 inserted=0 rejected=0` |
| `npm run pmci:audit:sports:packet` | PARTIAL — `semantic_violations=0` ✅ but `unknown_sport=38707` ❌ and `stale_active=19222` ❌ |
| PMCI API port 3001 | FAIL — no listener (`lsof -iTCP:3001` returned empty) |

**Root cause interpretation:**
The proposer returns `considered=0` because it has no valid sports candidates to evaluate. The two verified blockers are:
1. `unknown_sport=38707` — sport-inference is not classifying the vast majority of ingested sports markets. The proposer likely filters or rejects `unknown` sport rows before evaluation.
2. `stale_active=19222` — markets past their close time still marked `active`, polluting the candidate pool.

The PMCI API outage (port 3001) is a separate operational issue — the observer and ingest pipeline are healthy.

**Branch state:** `fix/e1-5-sports-proposer-2026-04-08` is 2 commits ahead of `main` (`52b413f`, `452a784`). Do NOT merge to main until E1.5 passes its acceptance gate.

---

## Three parallel tracks

Tracks A and B can start simultaneously. Track C is non-blocking hygiene.

---

## Track A — E1.5 Proposer Fix (critical path, blocks E2)

**Goal:** Make `npm run pmci:propose:sports` produce `considered > 0` and eventually ≥5 accepted cross-platform sports pairs with `semantic_violations=0`.

### A1 — Diagnose why `considered=0`

**Plumbo tasks (read only, no edits yet):**
- Read `scripts/review/pmci-propose-links-sports.mjs` (or wherever `pmci:propose:sports` maps in `package.json`)
- Identify the SQL query or filter that selects candidate markets
- Check whether it filters on `sport != 'unknown'` or requires non-null `sport`
- Print the candidate query to understand what it expects

**Claude verification:**
```bash
# Check what script pmci:propose:sports calls
cat package.json | grep "propose:sports"
# Count sports markets by sport code in DB
npm run pmci:smoke
```

**Hard gate A1:** Plumbo reports the exact reason `considered=0` (filter condition, empty join result, or sport field mismatch). No code changes yet.

---

### A2 — Fix sport-inference to clear `unknown_sport=38707`

**Context:** `lib/ingestion/services/sport-inference.mjs` infers sport code from market titles. The audit packet shows 38,707 markets with `sport='unknown'`. Most are likely Polymarket markets whose tag-based titles don't match existing word-boundary patterns.

**Plumbo tasks:**
- Read `lib/ingestion/services/sport-inference.mjs` — understand current patterns
- Read `docs/latest-sports-audit-packet.json` — identify the specific title patterns that fall through to `unknown`
- Run a DB query to sample unknown-sport market titles:
  ```sql
  SELECT title, provider, COUNT(*) ct
  FROM pmci.provider_markets
  WHERE category='sports' AND (sport IS NULL OR sport='unknown')
  GROUP BY title, provider
  ORDER BY ct DESC
  LIMIT 50;
  ```
- Add patterns to `sport-inference.mjs` to cover the top buckets (focus on patterns representing >1% of unknown count)
- After editing, run ingestion to re-classify:
  ```bash
  npm run pmci:ingest:sports
  ```

**Claude verification:**
```bash
npm run pmci:audit:sports:packet
# Target: unknown_sport < 1000 (from 38,707)
npm run verify:schema
npm run pmci:smoke
```

**Hard gate A2:** `unknown_sport` in audit packet drops to < 1,000. `verify:schema` and `pmci:smoke` still pass.

---

### A3 — Clear stale-active backlog (19,222 rows)

**Context:** 19,222 markets are marked `status='active'` but have `game_date` or `close_time` in the past. These pollute the proposer candidate pool and the audit gate.

**Plumbo tasks:**
- Check if `scripts/review/pmci-clear-stale-proposals.mjs` or a dedicated stale-market script handles this
- If not, write a targeted update (do NOT bulk-inactivate without the inactive-guard check):
  ```bash
  # Run inactive-guard check FIRST — required by invariants
  npm run pmci:check-coverage
  ```
- Then apply stale cleanup only to sports category markets past their `game_date` AND with `close_time < NOW()`:
  ```sql
  -- Plumbo: add this as a dry-run first (SELECT only), then UPDATE after Claude confirms count
  SELECT COUNT(*) FROM pmci.provider_markets
  WHERE category='sports'
    AND status='active'
    AND close_time IS NOT NULL
    AND close_time < NOW();
  ```
- Run the update only after Claude confirms the dry-run count matches the 19,222 figure

**Claude verification:**
```bash
npm run pmci:audit:sports:packet
# Target: stale_active = 0
npm run pmci:smoke
```

**Hard gate A3:** `stale_active=0` in audit packet. `pmci:smoke` still passes.

---

### A4 — Rerun proposer and verify non-zero candidates

**Plumbo tasks:**
- Run the sports proposer:
  ```bash
  npm run pmci:propose:sports
  ```
- Report exact output: `considered=`, `inserted=`, `rejected=` counts

**Claude verification:**
```bash
npm run pmci:propose:sports
# Target: considered > 0
```

**Hard gate A4:** `considered > 0`. If still 0 after A2 + A3, Plumbo digs into the proposer logic for sport-field join condition before advancing.

---

### A5 — Sports acceptance gate (E1.5 closeout)

**Plumbo tasks:**
- Review proposed pairs (Claude will run review queue)
- Accept pairs that pass semantic checks
- Run final audit packet:
  ```bash
  npm run pmci:audit:sports:packet
  ```

**Claude verification:**
```bash
npm run pmci:audit:sports:packet
npm run verify:schema
npm run pmci:smoke
```

**Hard gate A5 (E1.5 complete):**
- `semantic_violations=0`
- `stale_active=0`
- `unknown_sport < 1,000`
- ≥5 accepted cross-platform sports pairs with `status='accepted'` in `market_links`

**On pass:** Claude commits the branch, merges to main, updates `docs/system-state.md` and `docs/roadmap.md`, and opens E2.

---

## Track B — PMCI API Port 3001 Recovery (operational, parallel with A)

**Goal:** PMCI API listening on port 3001 with `/v1/health/slo` returning 200.

### B1 — Diagnose and restart

**Plumbo tasks:**
- Check what `npm run api:pmci` does:
  ```bash
  cat package.json | grep "api:pmci"
  ```
- Start the PMCI API:
  ```bash
  npm run api:pmci
  ```
- Watch startup logs for port binding errors or crash reason
- Report first 30 lines of startup output

**Claude verification:**
```bash
lsof -iTCP:3001 -sTCP:LISTEN -n -P
curl -s http://localhost:3001/v1/health/slo | head -c 500
```

**Hard gate B1:** Port 3001 has a listener AND `/v1/health/slo` returns HTTP 200.

### B2 — Diagnose if B1 startup fails

If `api:pmci` crashes on start:
- Read the last 50 lines of startup stderr for the crash message
- Check if it's a missing env var, port conflict, or Fastify boot error
- Fix the specific error (do NOT edit `.env` — print proposed changes to stdout only)
- Rerun B1

---

## Track C — Live Audit Script (hygiene, non-blocking)

**Goal:** Create `scripts/run_pmci_live_audit.sh` so future audits don't require manually stitching 8 commands together.

### C1 — Create the audit shell script

**Plumbo tasks:**
Create `scripts/run_pmci_live_audit.sh` with the following stitched checks:

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

echo "=== PMCI LIVE AUDIT $(date -u '+%Y-%m-%dT%H:%M:%SZ') ==="
echo ""

echo "--- Schema ---"
npm run verify:schema

echo ""
echo "--- Smoke ---"
npm run pmci:smoke

echo ""
echo "--- API Port 3001 ---"
lsof -iTCP:3001 -sTCP:LISTEN -n -P 2>/dev/null && echo "PORT_3001_LISTENING" || echo "PORT_3001_NOT_LISTENING"
curl -s --max-time 3 http://localhost:3001/v1/health/slo 2>/dev/null | head -c 300 || echo "API_UNREACHABLE"

echo ""
echo "--- Sports Proposer ---"
npm run pmci:propose:sports 2>&1 | tail -5

echo ""
echo "--- Sports Audit Packet ---"
npm run pmci:audit:sports:packet 2>&1 | tail -10

echo ""
echo "=== AUDIT COMPLETE ==="
```

- Make it executable: `chmod +x scripts/run_pmci_live_audit.sh`
- Add to `package.json`: `"pmci:audit:live": "bash scripts/run_pmci_live_audit.sh"`
- Test it: `npm run pmci:audit:live`

**Hard gate C1:** Script runs without error and produces audit output covering all 5 sections.

---

## Verification sequence after all tracks complete

Run in order after A5 + B1 + C1 pass:

```bash
npm run verify:schema
npm run pmci:smoke
npm run pmci:audit:sports:packet
bash scripts/run_pmci_live_audit.sh
curl -s http://localhost:3001/v1/health/slo
```

All must pass before the branch is merged to main.

---

## Message length guidance for Plumbo

| Session state | What to send |
|---------------|-------------|
| New session (fresh context) | Send full A1 block — context + task + hard gate |
| After A1 confirmation | Send A2 task only (3–5 lines) |
| After each hard gate passes | Send next single track step |
| Context-saturated (↓4 tokens) | Stop; use Desktop Commander for remaining shell commands |

**First message template for a new Plumbo session:**

```
[PMCI invariants: no .env writes; run verify:schema after migrations; new routes in src/api.mjs only; inactive-guard before bulk market changes]

We're fixing E1.5 (sports proposer acceptance). The proposer returns considered=0. Two verified blockers: unknown_sport=38707 and stale_active=19222 from the latest audit packet at docs/latest-sports-audit-packet.json.

Start with A1: read the script that pmci:propose:sports calls (check package.json), then read that script and find the exact filter causing considered=0. Report the filter condition — no code changes yet.
```

---

## Reference files

| File | Purpose |
|------|---------|
| `docs/system-state.md` | Live system state — check before asserting current reality |
| `docs/roadmap.md` | Phase gates and acceptance criteria |
| `docs/latest-sports-audit-packet.json` | Last verified audit packet (38707 unknown_sport, 19222 stale_active) |
| `lib/ingestion/services/sport-inference.mjs` | Sport classification patterns — primary fix target for A2 |
| `lib/matching/proposal-engine.mjs` | Proposal engine — check sports candidate filter for A1 |
| `scripts/review/pmci-clear-stale-proposals.mjs` | Stale proposal cleanup — check if reusable for A3 |
| `docs/phase-e1-5-sport-inference-and-runtime-plan.md` | Prior E1.5 plan context |

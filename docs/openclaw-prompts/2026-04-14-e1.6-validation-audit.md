# E1.6 Validation Audit Prompt
# Target: Claude Code (new session dispatch)
# Purpose: Verify all E1.6 deliverables before declaring E2 unblocked

---

## Prompt (paste into new Claude Code / Cowork session)

```
## Context (carry forward)
- Repo: ~/prediction-machine (Node.js ESM, Fastify, Supabase/Postgres)
- Phase E1.6 (sports execution-readiness sprint) was executed the week of 2026-04-14
- All 7 steps were declared complete by the prior session
- Main branch is at commit 28db86f (spread dashboard)
- One operator action is pending: OBSERVER_DB_DISCOVERY=1 must be added to .env and observer restarted
- DO NOT auto-write to .env — this is a CLAUDE.md invariant

## Your task
You are a senior backend engineer performing a post-sprint validation audit on the prediction-machine repo. Verify that all E1.6 exit criteria are actually met. Be skeptical — check current DB state, not the prior session's claims.

Run the following checks IN ORDER. After each check, output: ✅ PASS, ❌ FAIL, or ⚠️ WARN — followed by the exact value observed and the exit criterion it must meet. Stop and ask before taking any corrective action.

---

### CHECK 1 — Sport Inference Backfill (Step 1 exit: unknown_sport < 500)
Run:
```sql
SELECT COUNT(*) FROM pmci.provider_markets
WHERE category = 'sports' AND (sport IS NULL OR sport = 'unknown')
  AND status IN ('open', 'active');
```
PASS if result < 500.

---

### CHECK 2 — Stale-Active Cleanup (Step 2 exit: stale_active < 100)
Run:
```sql
SELECT COUNT(*) FROM pmci.provider_markets pm
WHERE pm.status IN ('open', 'active')
  AND pm.category = 'sports'
  AND (
    (pm.close_time IS NOT NULL AND pm.close_time < NOW())
    OR (
      pm.close_time IS NULL
      AND NOT EXISTS (
        SELECT 1 FROM pmci.market_snapshots ms
        WHERE ms.provider_market_id = pm.id
          AND ms.observed_at > NOW() - INTERVAL '7 days'
      )
    )
  );
```
PASS if result < 100.

---

### CHECK 3 — Semantic Violations (Step 3 exit: semantic_violations = 0)
Run:
```bash
npm run pmci:audit:sports:packet 2>&1 | grep -E "semantic_violations|violations"
```
PASS if semantic_violations = 0.

---

### CHECK 4 — Sports Link Count (Step 4 exit: current_links (sports) >= 200)
Run:
```sql
SELECT COUNT(*) FROM pmci.market_links ml
JOIN pmci.provider_markets pm1 ON pm1.id = ml.kalshi_market_id
WHERE ml.status = 'active' AND pm1.category = 'sports';
```
PASS if result >= 200.

---

### CHECK 5 — E1 Strict Audit Gate (all sub-gates green)
Run:
```bash
npm run pmci:audit:sports:packet 2>&1 | tail -30
```
PASS if output shows all strict-audit gates GREEN with no red. Capture the exact gate summary lines.

---

### CHECK 6 — Verify Schema (invariant: must pass after any migration)
Run:
```bash
npm run verify:schema
```
PASS if exits 0 with no errors.

---

### CHECK 7 — Smoke Test (API health)
Run:
```bash
npm run pmci:smoke
```
PASS if exits 0.

---

### CHECK 8 — Test Suite (Step 1–7 supporting tests)
Run:
```bash
npm test 2>&1 | tail -20
```
PASS if all tests pass (look for "passing" count, zero failures). Note exact test count.

---

### CHECK 9 — Spread Dashboard exists and references DB values
Verify:
```bash
ls -lh docs/spread-dashboard.html
head -5 docs/spread-dashboard.html
```
PASS if file exists and is > 10KB.

---

### CHECK 10 — Competitive Baseline doc exists
Verify:
```bash
ls -lh docs/competitive-baseline.md
wc -l docs/competitive-baseline.md
```
PASS if file exists and is > 50 lines.

---

### CHECK 11 — OBSERVER_DB_DISCOVERY status (operator action gate)
Run:
```bash
grep -i "OBSERVER_DB_DISCOVERY" .env 2>/dev/null || echo "NOT SET"
grep -i "OBSERVER_DB_DISCOVERY" observer.mjs | head -5
```
PASS on the code check if observer.mjs contains the OBSERVER_DB_DISCOVERY logic.
WARN (not FAIL) if .env does not have it set — this is a known pending operator action.

---

### CHECK 12 — Kalshi prices flowing on linked pairs
Run:
```sql
SELECT COUNT(DISTINCT ml.id) as linked_with_kalshi_price
FROM pmci.market_links ml
JOIN pmci.provider_markets pm1 ON pm1.id = ml.kalshi_market_id
JOIN pmci.market_snapshots ms ON ms.provider_market_id = ml.kalshi_market_id
WHERE ml.status = 'active'
  AND pm1.category = 'sports'
  AND ms.observed_at > NOW() - INTERVAL '24 hours'
  AND ms.best_bid_yes IS NOT NULL;
```
PASS if result > 0. Note exact count.

---

### CHECK 13 — Polymarket prices (expected: 0 until operator enables OBSERVER_DB_DISCOVERY)
Run:
```sql
SELECT COUNT(DISTINCT ml.id) as linked_with_poly_price
FROM pmci.market_links ml
JOIN pmci.provider_markets pm2 ON pm2.id = ml.polymarket_market_id
JOIN pmci.market_snapshots ms ON ms.provider_market_id = ml.polymarket_market_id
WHERE ml.status = 'active'
  AND pm2.category = 'sports'
  AND ms.observed_at > NOW() - INTERVAL '24 hours'
  AND ms.best_bid_yes IS NOT NULL;
```
If result = 0: WARN — expected until OBSERVER_DB_DISCOVERY=1 is enabled.
If result > 0: PASS — bilateral prices are flowing.

---

## Final output format

After all 13 checks, output a table:

| Check | Gate | Observed | Result |
|-------|------|----------|--------|
| 1 — unknown_sport | < 500 | [value] | ✅/❌/⚠️ |
| 2 — stale_active | < 100 | [value] | ✅/❌/⚠️ |
| 3 — semantic_violations | = 0 | [value] | ✅/❌/⚠️ |
| 4 — sports link count | ≥ 200 | [value] | ✅/❌/⚠️ |
| 5 — strict audit | all green | [summary] | ✅/❌/⚠️ |
| 6 — verify:schema | pass | [exit code] | ✅/❌/⚠️ |
| 7 — pmci:smoke | pass | [exit code] | ✅/❌/⚠️ |
| 8 — npm test | all pass | [N passing] | ✅/❌/⚠️ |
| 9 — spread dashboard | exists >10KB | [size] | ✅/❌/⚠️ |
| 10 — competitive baseline | exists >50 lines | [lines] | ✅/❌/⚠️ |
| 11 — OBSERVER_DB_DISCOVERY | code merged | [found/not found] | ✅/❌/⚠️ |
| 12 — Kalshi prices flowing | > 0 pairs | [count] | ✅/❌/⚠️ |
| 13 — Polymarket prices | > 0 (or warn) | [count] | ✅/❌/⚠️ |

Then output one of:
- **E1.6 VALIDATED — E2 UNBLOCKED** (if checks 1–12 all ✅, check 13 is ⚠️ is acceptable)
- **E1.6 PARTIAL — [list of failing checks]** (if any ❌)

Do NOT take corrective action. Report only. Stop and ask before running anything destructive.
```

node_modules
.env
.env.local
*.log
logs/
.pmci_kalshi_universe_checkpoint.json
.supabase
supabase/.temp
.DS_Store

# Live audit snapshots (ephemeral)
pmci-live-audit-*/

# Benchmark run output (may include API payloads)
output/benchmark/last-run.json

.pmci_kalshi_crypto_checkpoint.json
.pmci_kalshi_economics_checkpoint.json

# Repo audit: daily JSON is noisy; keep weekly markdown unless you prefer otherwise
state/repo-audit/daily/*.json
